#! /usr/bin/env node
"use strict";
// Copyright 2024 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// Command line tool to test the JavaScript implementation of Magika. Please use
// the official command line tool (`pip install magika`) for normal use.
// To run this, you need to install the optional dependencies too.
const chalk_1 = __importDefault(require("chalk"));
const commander_1 = require("commander");
const fs = __importStar(require("fs"));
const promises_1 = require("fs/promises");
const magika_node_js_1 = require("./magika-node.js");
commander_1.program
    .description("Magika JS - file type detection with ML. https://google.github.io/magika")
    .option("--json-output", "Format output in JSON")
    .option("--model-url <model-url>", "Model URL", magika_node_js_1.MagikaNode.MODEL_URL)
    .option("--model-path <model-path>", "Modle file path")
    .option("--model-config-url <model-config-url>", "Model config URL", magika_node_js_1.MagikaNode.MODEL_CONFIG_URL)
    .option("--model-config-path <model-config-path>", "Model config file path")
    .option("--by-stream", "Identify file via stream, not via bytes")
    .option("--debug", "Output debug information")
    .argument("<paths...>", "Paths of the files to detect");
commander_1.program.exitOverride();
try {
    commander_1.program.parse(process.argv);
}
catch (error) {
    if (error instanceof commander_1.CommanderError &&
        error.code === "commander.helpDisplayed") {
        // Help was already displayed by commander, so just exit cleanly.
        process.exit(0);
    }
    else {
        // There was an error parsing the arguments, let's print the help.
        try {
            commander_1.program.help();
        }
        catch (error) {
            // Avoid that commander shows some weird exception.
            process.exit(1);
        }
    }
}
const flags = commander_1.program.opts();
(() => __awaiter(void 0, void 0, void 0, function* () {
    const magika = yield magika_node_js_1.MagikaNode.create({
        modelURL: flags.modelUrl,
        modelPath: flags.modelPath,
        modelConfigURL: flags.configUrl,
        modelConfigPath: flags.configPath,
    });
    yield Promise.all(commander_1.program.args.map((path) => __awaiter(void 0, void 0, void 0, function* () {
        let data = null;
        try {
            data = yield (0, promises_1.readFile)(path);
        }
        catch (error) {
            console.error("Skipping file", path, error);
        }
        if (data != null) {
            if (flags.byStream) {
                const magika_result_by_stream = yield magika.identifyStream(fs.createReadStream(path), data.length);
                if (flags.jsonOutput) {
                    console.log(path, magika_result_by_stream);
                }
                else if (flags.debug) {
                    console.log(chalk_1.default.yellow(path), "by_stream", chalk_1.default.green(magika_result_by_stream.prediction.dl.label, magika_result_by_stream.prediction.output.label), chalk_1.default.white(magika_result_by_stream.prediction.score));
                }
                else {
                    console.log(chalk_1.default.yellow(path), chalk_1.default.green(magika_result_by_stream.prediction.output.label), chalk_1.default.white(magika_result_by_stream.prediction.score.toFixed(3)));
                }
            }
            else {
                const magika_result_by_path = yield magika.identifyBytes(data);
                if (flags.jsonOutput) {
                    console.log(path, magika_result_by_path);
                }
                else if (flags.debug) {
                    console.log(chalk_1.default.yellow(path), "by_path", chalk_1.default.green(magika_result_by_path.prediction.dl.label, magika_result_by_path.prediction.output.label), chalk_1.default.white(magika_result_by_path.prediction.score));
                }
                else {
                    console.log(chalk_1.default.yellow(path), chalk_1.default.green(magika_result_by_path.prediction.output.label), chalk_1.default.white(magika_result_by_path.prediction.score.toFixed(3)));
                }
            }
        }
    })));
}))();
